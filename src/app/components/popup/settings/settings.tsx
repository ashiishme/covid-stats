import React, { FC } from 'react';

const Settings: FC = () => {
    return (
        <div className="ashiishme-covid-settings">
            <p>Settings</p>
        </div>
    );
};

export default Settings;
